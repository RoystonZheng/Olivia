# 第一期需求

## 背景
现有仓库里的全息界面原型不作为产品核心。Olivia 的第一版目标不是炫酷展示，而是把手机变成一个能长期记住用户偏好、能按语境推荐附近餐厅、能在需要时直接帮用户打电话问清楚情况的私人助手。

## 目标
把一台华为手机做成一个安静待命的个人助理：

- 用户戴着耳机，可以直接开口和 Olivia 对话
- Olivia 默认不打扰用户，只有被唤醒时才回应
- Olivia 记住用户的口味、预算、距离偏好、娱乐偏好
- Olivia 根据当前位置推荐附近的餐厅
- 用户说“打电话”时，Olivia 直接发起商家电话
- Olivia 的记忆在云端同步，后续可在其他设备延续
- 第一阶段优先基于华为手机与鸿蒙系统实机验证，目标环境以华为 Pura 系列 / HarmonyOS 6.1.1 为准

## 第一阶段范围
### 必做
- 华为 HarmonyOS / 鸿蒙端语音对话
- 账号登录与云端记忆
- 结构化偏好记录
- 基于定位与高德 POI API 的附近餐厅推荐
- 餐厅结果解释：距离、价格、招牌菜、差评/风险点
- 回退式追问：换口味、换预算、换一家
- 商家电话拨号与通话任务记录

### 暂不做
- 自动付款
- 自动订票
- 自动下单外卖
- iPhone / Mac 原生客户端
- 主动打扰式提醒
- 全自动长期后台 agent
- 全息投影/HUD 界面

## 体验原则
- 安静：不主动插话，不无故弹窗，不自己播报
- 像朋友：少术语，多自然表达
- 一次只问一个关键问题
- 先给可用答案，再允许继续细化
- 所有记忆都可被用户修改
- 所有外部动作都可追踪

## 核心场景
### 场景 1：中午吃什么
用户说：“中午吃什么。”
Olivia 先问口味偏好，或给几类有代表性的口味让用户选。

用户选定口味后，Olivia 结合：
- 当前定位
- 预算
- 历史偏好
- 当前时间
- 距离
- 餐厅评分、人均、标签、电话等 POI 信息
- 可推断的差评/风险信号

返回 3 到 5 家候选，包含：
- 名称
- 距离
- 人均价格
- 招牌菜
- 为什么推荐
- 简短差评/风险提示

用户说“换口味”，返回上一步重新选口味。  
用户说“打电话”，视为已经完成业务确认，直接对选中的商家发起电话。

### 场景 2：想问商家情况
用户说：“给这家打电话问一下有没有位。”
Olivia 创建通话任务，拨号，记录结果，并把关键信息写回记忆。

### 场景 3：不想吃饭，想找活动
这一类作为第二阶段扩展，不进入 V1 交付范围。

## 产品边界
Olivia 不做“一个什么都自动完成的黑盒”。

第一版的原则是：
- 用户说要做什么，Olivia 才做
- Olivia 可以帮忙判断和推进，但不越权替用户付款或提交最终订单
- 需要高风险动作时，默认保留可中断和可回退的路径

## 记忆设计
### 结构化记忆
长期保存这些字段：
- 口味偏好
- 不喜欢的口味
- 预算区间
- 可接受距离
- 喜欢的娱乐类型
- 常去区域
- 对餐厅环境的偏好
- 对排队、辣度、油腻度、安静程度的偏好

### 会话记忆
保存最近一次或最近几次对话里的：
- 当前口味
- 当前预算
- 当前选中的候选餐厅
- 当前是否在“打电话询问”流程里

### 反馈记忆
用户说“喜欢”“不喜欢”“太贵了”“太远了”“味道一般”等反馈时，系统更新偏好权重。

## 架构建议
### 客户端
华为 HarmonyOS / 鸿蒙 App 作为主入口：
- 麦克风输入
- 语音唤醒或耳机触发
- TTS 播报
- 通话动作触发
- 本地会话状态

### 后端
统一云端服务负责：
- 用户身份与同步
- 偏好与历史存储
- 对话编排
- 地点/餐厅检索
- 推荐排序
- 通话任务记录

### 数据层
建议分三类：
- 用户与偏好表
- 对话与任务日志
- 可检索记忆索引

### 外部能力
- 华为 Location Kit：获取用户当前位置，只解决“用户在哪”
- 高德 Web 服务 / POI API：查询附近餐厅、人均、评分、电话、标签、距离等信息
- 高德 URI API 或地图 SDK：打开地点、路线规划或导航
- 华为系统电话能力：发起商家电话
- 语音识别
- 语音合成

## 技术实现路线
### 总原则
第一期采用“API 优先，模拟人工 GUI 兜底”的路线。

Olivia 不应该像人一样打开高德地图、美团或大众点评，再点击搜索框、输入关键词、读取屏幕结果。第一期的核心链路要尽量通过稳定接口完成：
- 查找餐厅：调用高德 POI API
- 获取当前位置：调用华为 Location Kit
- 打开路线或导航：调用高德 URI API 或地图 SDK
- 发起电话：调用鸿蒙系统电话能力
- 推荐解释：由 Olivia 的后端根据用户记忆和 POI 数据生成

只有当某个高价值动作没有 API、没有 URI 跳转入口、也无法通过系统能力完成时，后续版本才考虑模拟人工 GUI。例如完整外卖下单、团购券购买、读取美团/大众点评完整评论区、跨 App 填表等。

### 定位与地点数据
Location Kit 只负责定位，不能提供美团、大众点评或高德那种餐厅消费决策信息。它能告诉 Olivia 用户当前在哪里，但不能直接告诉 Olivia 餐厅的人均、评分、评价、招牌菜或差评。

餐厅检索与推荐数据第一期以高德 POI API 为主：
- 周边搜索：按当前位置、半径、关键词、餐饮类型查询候选餐厅
- 详情字段：优先使用距离、地址、电话、标签、评分、人均消费等可用字段
- 数据缺失：如果某个餐厅没有人均、评分或电话，Olivia 要明确降低推荐权重，或在回答中说明“不确定”
- 招牌菜：优先使用高德返回的标签、特色字段或商家类目信息；没有明确数据时不能装作确定
- 差评/风险：第一期不强依赖完整评论数据，先用低评分、超预算、距离远、电话缺失、营业状态不确定、热门时段可能排队等弱信号生成风险提示

美团和大众点评的数据更适合判断真实口碑、排队、服务、环境和具体差评，但这类平台通常不会开放完整评论 API。第一期不把它们作为硬依赖，避免产品从一开始就卡在数据授权和自动化风险上。

### 高德交互方式
高德相关能力分三层：
- 查询：用高德 Web 服务 / POI API，不模拟 GUI
- 展示与导航：用高德 URI API 或地图 SDK 拉起高德地图
- 复杂 App 内动作：如果未来要操作高德 App 内部页面，只有在没有 API 或 URI 能力时才考虑 GUI 自动化

Olivia 自己负责对话、筛选、排序和解释，高德只作为地点与地图能力提供方。

### 电话交互方式
电话调用采用“语音确认前置，系统确认兜底”的设计。

当 Olivia 主动问“要我现在打电话问一下吗？”而用户回答“打电话”“帮我问问”“可以”时，就视为用户已经在业务层确认拨打，不再重复追问。

执行流程：
1. 判断用户是否已经明确表达要拨打
2. 检查选中餐厅是否有商家电话
3. 检查设备是否支持通话能力
4. 调用鸿蒙系统电话能力或 `tel:` 链接发起拨号
5. 如果系统出于安全原因弹出拨号确认界面，Olivia 提示用户点一下系统拨号按钮
6. 如果没有电话、拨号失败或商家无人接听，Olivia 回到推荐列表，让用户换一家或换口味

Olivia 不绕过系统安全确认，也不尝试后台静默拨号。系统层确认属于手机安全机制，产品层确认由 Olivia 的对话完成。

### GUI 自动化边界
第一期不使用模拟人工 GUI 完成核心推荐链路。

后续版本可以在这些场景研究 GUI 自动化：
- 外卖 App 选择店铺、加购、提交订单前确认
- 票务 App 搜索班次、筛选座位、提交订单前确认
- 社交 App 读取消息草稿、辅助回复
- 平台没有开放 API，但用户明确授权 Olivia 代为操作

GUI 自动化必须满足：
- 用户明确授权
- 高风险动作前可中断
- 不自动付款
- 不绕过验证码和平台安全规则
- App 改版后有降级方案

## 对话与推荐流程
1. 用户唤醒 Olivia
2. 语音转文字
3. 识别意图：吃什么 / 打电话 / 换口味 / 其它
4. 读取用户偏好与当前位置
5. 用高德 POI API 查询附近候选
6. 按预算、距离、口味、评分、风险信号排序
7. 生成自然语言解释
8. 用户继续追问或发起电话
9. 更新记忆与日志

## 排序规则
先做硬过滤，再做软排序。

### 硬过滤
- 不在可达范围内
- 超出预算
- 已关闭
- 明显不符合口味

### 软排序
- 更符合用户历史偏好
- 距离更近
- 评分更稳
- 差评风险更低
- 当前场景更合适，例如约会、午餐、独自用餐

## 通话流程
第一版的电话能力按“用户语音确认后，发起商家电话并记录结果”设计。

当用户说“打电话”“帮我问问”“问一下有没有位”时，Olivia 视为已经确认拨打。系统如果仍然要求点击拨号按钮，则作为手机系统的安全确认处理。

如果后续设备能力和权限允许，Olivia 可以继续升级为：
- 帮用户读出预设问题
- 记录商家回答
- 汇总为可读结论

如果某些系统限制导致无法完整自动化，则回退为：
- 直接拨号
- 交给用户接手
- Olivia 记录结果

## 非功能要求
- 默认安静，不主动打扰
- 语音响应尽量短
- 推荐结果要稳定，不要前后飘
- 云端同步要可靠
- 用户可随时修改偏好
- 日志可追踪，便于调试

## 测试重点
- 唤醒后是否稳定进入对话
- 是否真的“没叫就不打扰”
- 记忆是否跨会话保留
- 口味变化后是否会回到上一层
- 推荐是否能按预算和距离过滤
- 高德 POI 数据缺失时是否能降级说明
- Location Kit 定位失败时是否能让用户手动指定区域
- 高德 URI / 地图拉起失败时是否能只给文字地址
- 电话任务是否能正确发起
- 系统弹出拨号确认时 Olivia 是否能正确提示
- 网络差、定位失败、商家无应答时是否能降级

## 验收标准
- 华为端可登录并稳定进入语音对话
- 用户问“中午吃什么”时，系统能先追问口味或给出代表性口味选项
- 用户选定口味后，系统能返回 3 到 5 家附近餐厅，并展示距离、价格和简单理由
- 用户说“换口味”时，系统能回到口味选择
- 用户说“打电话”时，系统能发起对商家电话并记录结果
- 系统在未被唤醒时不主动打扰用户

## 里程碑
### M1
华为端能登录、唤醒、对话、保存偏好。

### M2
能稳定完成“中午吃什么”推荐闭环。

### M3
能对选中商家发起电话并记录结果。

### M4
扩展到附近活动推荐。

### M5
扩展到订票、外卖、消息等高权限动作。

## 风险
- Android 设备权限与厂商限制
- 常驻待命的电量消耗
- 高德 POI 数据质量与字段缺失
- 无法获取美团/大众点评完整评价导致差评信息不足
- 电话调用受系统安全确认限制，无法保证后台静默拨号
- GUI 自动化受 App 改版、验证码、平台规则影响，不能作为第一期核心依赖
- 记忆错误导致“像机器人”或“自作主张”

## 结论
Olivia V1 先不追求“大而全”，而是先把“像一个懂你的朋友一样推荐吃什么，并在你要的时候直接帮你打电话”这条链路打穿。这样最容易真的落地，也最能验证这个产品值不值得继续做大。
